Ce dossier est le contenu à pousser dans votre projet GitLab local.

- Il contient l’app `wil42/playground` (port 8888) en version `v1`.
- Pour passer à `v2`, éditez `deployment.yaml` et remplacez `wil42/playground:v1` par `wil42/playground:v2`, puis commit+push.
